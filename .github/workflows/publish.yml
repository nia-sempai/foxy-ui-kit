name: Publish package

# Публикация по тегу v*. GitHub Packages принимает только scoped-имена
# (@owner/name), а кит публикуется под коротким `foxy-ui-kit`, поэтому цель
# одна — npm. Из репозитория пакет ставится и без публикации:
# `yarn add foxy-ui-kit@github:nia-sempai/foxy-ui-kit` (dist собирает prepack).
on:
  push:
    tags: ['v*']
  workflow_dispatch:

jobs:
  npm:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org
      - run: corepack enable
      - run: yarn install --immutable
      - run: yarn build

      # Без токена публикация невозможна: сообщаем об этом явно, иначе шаг
      # падает с невнятной ошибкой авторизации реестра.
      - name: Check npm token
        id: token
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          if [ -z "$NPM_TOKEN" ]; then
            echo "::warning::Секрет NPM_TOKEN не задан — публикация в npm пропущена."
            echo "Добавьте токен: Settings → Secrets and variables → Actions → NPM_TOKEN"
            echo "present=false" >> "$GITHUB_OUTPUT"
          else
            echo "present=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish to npm
        if: steps.token.outputs.present == 'true'
        run: yarn npm publish --access public
        env:
          YARN_NPM_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      # Артефакт нужен всегда: даже без публикации по тегу можно скачать
      # собранный пакет из вкладки Actions.
      - name: Pack tarball
        run: yarn pack --out foxy-ui-kit-%v.tgz
      - uses: actions/upload-artifact@v4
        with:
          name: package
          path: foxy-ui-kit-*.tgz
